@using WarehouseManagementSystem.Extensions
@model Warehouse.ViewModels.Admin.OrderCurrencyUnitIdSelectViewModel
@{
    var categoryList = new SelectList((List<Warehouse.ViewModels.Admin.CurrencyUnitListViewModel>)ViewData["CurrencyUnits"], "Id", "Name").ToList();
    categoryList.Add(new SelectListItem()
    {
        Value = string.Empty,
        Text = "Para Birimi Seçiniz"
    });
}
<div class="form-group form-group-sm col-xs-12 col-sm-12 col-md-12 col-lg-12  ">
    <div class="input-group input-group-sm">
        <span class="input-group-addon as-label required">
            <strong> @Html.DisplayNameFor(m => m.CurrencyUnitId)</strong>
        </span>

        @Html.DropDownListFor(a => a.CurrencyUnitId, categoryList.OrderBy(a => a.Value), new { @class = "form-control" })
    </div>
    @Html.ValidationMessageFor(m => m.CurrencyUnitId, "", new { @class = "text-danger" })
</div>